# Optional live provider (OpenAI-compatible). Leave unset for the $0 mock path.
# SPECTRACE_BASE_URL=http://127.0.0.1:11434/v1
# SPECTRACE_API_KEY=
# SPECTRACE_MODEL=mock-local

# Optional TypeSafe AI Jev (trajectory grader / step router). Never required for tests.
# TYPESAFE_API_KEY=
# SPECTRACE_JEV_URL=https://api.typesafe.ai/v1/systemone
# SPECTRACE_JEV_MODEL=jev-latest
